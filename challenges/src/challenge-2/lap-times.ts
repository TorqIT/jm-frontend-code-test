/**
 * Lap Times
 * 
 * Given the array of lap time objects, please return a list of the fastest time for each driver, sorted by time ascending.
 * 
 * @param val 
 * @returns 
 */

export type LapTime = {
  driver: string;
  lapTime: number;
}

export function lapTimes(laptimes: LapTime[]): LapTime[] {
  return [];
}